extends TextEdit

# Called when the node enters the scene tree for the first time.
func _ready():
	release_focus()


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
#	if has_focus():
#		get_node("/Camera3D").player_typing = true
#	else:
#		get_node("/Camera3D").player_typing = false
	pass
